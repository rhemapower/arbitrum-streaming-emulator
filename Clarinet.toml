[project]
name = "arbitrum-streaming-emulator"
authors = ["Anonymous Developer"]
description = "A decentralized token streaming smart contract for time-based value distribution"
telemetry = false
requirements = []
[contracts.stream-emulator]
path = "contracts/stream-emulator.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
